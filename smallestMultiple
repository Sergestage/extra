function smallestMultiple() {
for (let i = 1; i < Number.MAX_SAFE_INTEGER; i++) {
  let divisible = true;
  for (let j = 1; j <= 20; j++) {
      if (i % j !== 0) {
        divisible = false;
      break;
   }
   }
 if (divisible) {
return i;
  }
 }
}

console.log(smallestMultiple());
